# Portfolio Premium Concept

This folder contains a standalone portfolio concept route at `/portfolio-premium/`.

It was intentionally added without replacing the existing root `index.html`, so you can review the alternate design direction separately.
